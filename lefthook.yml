# lefthook.yml

pre-commit:
  parallel: true
  jobs:
    - run: pnpm exec biome format --write --staged --no-errors-on-unmatched
      stage_fixed: true

pre-push:
  jobs:
    - run: pnpm run lint && pnpm run typecheck
